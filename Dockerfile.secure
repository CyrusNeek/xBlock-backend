FROM python:3.11-slim

WORKDIR /app

# Copy only the secure server
COPY secure_server.py /app/

# Make the script executable
RUN chmod +x /app/secure_server.py

# Expose the port
EXPOSE 8080

# Run the secure server directly
CMD ["python", "secure_server.py"]
